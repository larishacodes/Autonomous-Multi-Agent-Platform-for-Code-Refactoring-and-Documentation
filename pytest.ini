[pytest]
# Register custom marks so pytest doesn't warn about unknown marks
markers =
    integration: marks tests as full pipeline integration tests (may be slow)

# Always show short tracebacks unless overridden
addopts = --tb=short

# Treat these warnings as ignored rather than errors
filterwarnings =
    ignore::UserWarning:langchain_core
    ignore::DeprecationWarning:pydantic